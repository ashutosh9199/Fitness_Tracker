apiVersion: v1
kind: PersistentVolume
metadata:
  name: fitness-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: /nfs/etcd-backup
    server: 172.31.0.32
